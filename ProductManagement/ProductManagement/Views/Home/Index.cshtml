@{
    ViewData["Title"] = "Welcome to Our Page";
}
<h2>@ViewData["Title"]</h2>
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card mt-5">
                <div class="card-body text-center">
                    
                    <p class="card-text">Please choose an option:</p>
                    <a class="btn btn-primary btn-block" href="@Url.Action("Register","Admin")">Sign In</a>
                    <a class="btn btn-secondary btn-block mt-3" href="@Url.Action("Login","User")" ">Login</a>
                </div>
            </div>
        </div>
    </div>
</div>
